<%- include('partials/header') %>

<main>
  <h1><%= title %></h1>

  <h2>Dashboard</h2>

    <p>Name: <%= name %></p>
    <p>Username: <%= email %></p>
    <p>Role: <%= role %></p>

    <% if (role === 'admin') { %>
        <p>
            <a href="/users" class="action-link">Manage Users</a>
        </p>
    <% } %>
</main>

<%- include('partials/footer') %>
